name: Test

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04-arm
    container: lopsided/archlinux:latest

    steps:
      - name: Hello world
        shell: bash
        run: |
          echo Hello World
          uname -a
